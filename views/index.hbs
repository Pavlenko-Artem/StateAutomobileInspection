<!DOCTYPE html>
<html lang="en">
{{> head}}
<body>
	<div class="wrapper">
		{{> navbar}}
		<main class="main">
			<div class="container">
				{{message}}
			</div>
		</main>
		{{> footer}}
	</div>

	{{> bootstrap.script}}
</body>
</html>